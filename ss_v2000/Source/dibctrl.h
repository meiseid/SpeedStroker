#ifndef __DIBCTRL_H__#define __DIBCTRL_H__#include <windows.h>#include "errcode.h"//Creatorextern ERRCODE OpenDIB ( LPCTSTR szFile,LPHANDLE lphDIB );extern HPALETTE CreateDibPalette (const HANDLE hbi);extern HBITMAP BitmapFromDib (const HANDLE hdib,const HPALETTE hpal);//Accessorextern WORD PaletteSize ( const LPBITMAPINFOHEADER lpbi);//Drawerextern BOOL DrawBitmap (HDC    hdc,INT    x,INT    y,const HBITMAP    hbm,DWORD rop);extern BOOL StretchDibBlt (    HDC hdc,    INT x,    INT y,    INT dx,    INT dy,    const HANDLE hdib,    INT x0,    INT y0,    INT dx0,    INT dy0,    LONG rop);    extern BOOL DibBlt (    HDC    hdc,    INT    x0,    INT    y0,    INT    dx,    INT    dy,    const HANDLE hdib,    INT    x1,    INT    y1,    LONG   rop);#endif // __DIBCTRL_H__